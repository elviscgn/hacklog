#include "tui.h"
#include <stdio.h>

/* Stub — will be replaced with full ncurses implementation on Day 5 */
int tui_run(const char *profile_name) {
    printf("hacklog TUI — profile: %s\n", profile_name);
    printf("TUI not yet implemented. Use 'hack list' to see entries.\n");
    return 0;
}
